# S59.7.6 설치 안내

기존 프로젝트 루트에 `main.py`만 덮어쓴 뒤 Railway에서 재배포합니다. `/data`와 환경변수는 변경하지 않습니다.

배포 후 확인:
`/version`, `/versionaudit`, `/paper`, `/learning`, `/review`, `/accuracytracker`, `/memoryhealth`, `/strategytrust`, `/errors`
