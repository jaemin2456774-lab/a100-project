# 설치 안내

1. 기존 프로젝트를 백업합니다.
2. ZIP의 `main.py`를 기존 프로젝트 루트의 `main.py`에 덮어씁니다.
3. `/data`, 환경변수, Runtime/Learning 파일은 삭제하거나 초기화하지 않습니다.
4. Railway에서 재배포합니다.
5. `/version`, `/versionaudit`, `/runtimehealth`, `/status`, `/commandmatrix`, `/crossengineaudit`, `/evidencereplay`, `/rcpreflight`, `/releasegate`, `/errors` 순서로 확인합니다.
