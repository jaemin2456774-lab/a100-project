# S59.7.14 설치 안내

Railway 저장소에 패치 파일을 덮어쓴 뒤 재배포합니다. `/data`는 삭제하지 않습니다.

배포 후:
1. `/version`
2. `/versionaudit`
3. `/championstability`
4. `/help`
5. `/errors`

Champion 본문과 Adaptive 블록의 Samples, EV, MDD가 같아야 합니다.
`/help`에서 카테고리 버튼과 명령 버튼을 눌러 기능을 순차 검수합니다.
