# V92.6 설치 후 점검 체크리스트

1. 시작 로그에 `A100 V92.6 LEARNING INTELLIGENCE & EXPANDED SAMPLING` 표시
2. `/help`에 V92.6과 기본 샘플링 Paper 20 · Shadow 60 표시
3. `/commands V92` 정상 출력 및 중복 응답 없음
4. `/intelligence BTC` 카드형 판결, Confidence, 근거, Similarity 표시
5. `/dashboard BTC` 별점, Memory, Similarity 표시
6. `/final BTC` 최종 판결과 판단 사유 표시
7. `/learningstatus` 목표 150, 최근 24시간, Shadow OPEN 표시
8. `/papershadowstatus` 최대값 60 표시
9. `/memory`, `/review`, `/consensus BTC`, `/gold BTC`, `/ai` 기존 기능 정상
10. 기존 OPEN·평가·Memory 데이터 복원 확인
11. 설치 후 4시간 내 자동 학습 리포트 1회 수신 확인
12. 응답 지연, 출력 잘림, 미지원 명령, 중복 알림 확인
